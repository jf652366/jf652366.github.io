# jf652366.github.io

个人博客项目，基于 [VitePress](https://vitepress.vuejs.org/) 构建，采用自定义轻量主题（可选：Valaxy / 其他）。

注意：本仓库已迁移为 VitePress。CI 使用 `npm ci` 且部署到 GitHub Pages。若部署失败，请在仓库 Secrets 中设置 `PERSONAL_TOKEN`（带 repo 权限）。

## 🛠️ 技术栈

- **Core**: Hexo 8.x
- **Theme**: NexT 8.x
- **Package Manager**: Yarn

## 🚀 快速开始

### 环境准备

确保本地已安装 [Node.js](https://nodejs.org/) 和 [Yarn](https://yarnpkg.com/)。

### 安装依赖

```bash
yarn install
```

### 启动本地服务器

```bash
yarn server
```
启动后访问 `http://localhost:4000` 即可预览。

## 📜 常用命令

| 命令 | 描述 |
| --- | --- |
| `yarn server` | 启动本地开发服务器 (同 `hexo server`) |
| `yarn build` | 生成静态文件 (同 `hexo generate`) |
| `yarn clean` | 清理缓存文件 (同 `hexo clean`) |
| `yarn deploy` | 部署到远程仓库 (同 `hexo deploy`) |

## 📂 项目结构

```text
├── _config.yml         # Hexo 核心配置文件
├── _config.next.yml    # NexT 主题配置文件
├── source/             # 博客文章与页面源码
│   ├── _posts/         # 文章目录
│   └── ...
├── docs/               # 项目文档
├── scaffolds/          # 文章模板
└── package.json        # 项目依赖配置
```

## ✨ 最近优化

- **URL 优化**: 集成 `hexo-abbrlink` 生成短链接。
- **本地搜索**: 集成 `hexo-generator-searchdb` 支持站内搜索。
- **体验优化**: 移除文章切换动画，优化滚动条体验。
- **文档规范**: 遵循 `.cursorrules` 进行代码提交与 AI 辅助配置。

## 📝 编写文章

新建文章：
```bash
npx hexo new "文章标题"
```
文章将生成在 `source/_posts/` 目录下。

## 📄 License

MIT
